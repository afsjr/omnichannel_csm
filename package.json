{
  "name": "omnichat-csm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "vercel-build": "npm install --include=dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.0",
    "jsonwebtoken": "^9.0.2"
  }
}